import React from 'react';
import ExampleCustomizer from './ExampleCustomizer';

export default (props: { code: string; Example: any }) => {
  const { Example, code } = props;
  return <div>2222</div>;
};
